import { Routes } from '@angular/router'; import { ResourceConfig } from '../../shared/models/resource-form.models'; import { ResourceDetailPage } from '../../shared/ui/resource-detail.page'; import { ResourceListPage } from '../../shared/ui/resource-list.page'; import { RehearsalSessionFormPage } from './pages/rehearsal-session-form.page'; import { RehearsalSessionService } from './services/rehearsal-session.service';
const resource:ResourceConfig={key:'rehearsal-sessions',singular:'prova',plural:'Prove',service:RehearsalSessionService,fields:[{key:'title',label:'Titolo',type:'text',required:true},{key:'date',label:'Data',type:'date',required:true},{key:'startTime',label:'Ora inizio',type:'time'},{key:'endTime',label:'Ora fine',type:'time'},{key:'rehearsalRoomId',label:'ID sala prove',type:'number'},{key:'notes',label:'Note',type:'textarea'}]};
export const REHEARSAL_SESSION_ROUTES:Routes=[{path:'',component:ResourceListPage,data:{resource}},{path:'new',component:RehearsalSessionFormPage,data:{resource}},{path:':id',component:ResourceDetailPage,data:{resource}},{path:':id/edit',component:RehearsalSessionFormPage,data:{resource}}];
