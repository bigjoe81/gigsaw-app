import { Routes } from '@angular/router'; import { ResourceConfig } from '../../shared/models/resource-form.models'; import { ResourceDetailPage } from '../../shared/ui/resource-detail.page'; import { ResourceListPage } from '../../shared/ui/resource-list.page'; import { RecordingSessionFormPage } from './pages/recording-session-form.page'; import { RecordingSessionService } from './services/recording-session.service';
const resource:ResourceConfig={key:'recording-sessions',singular:'sessione di registrazione',plural:'Sessioni di registrazione',service:RecordingSessionService,fields:[{key:'title',label:'Titolo',type:'text',required:true},{key:'date',label:'Data',type:'date',required:true},{key:'songId',label:'ID brano',type:'number'},{key:'location',label:'Luogo',type:'text'},{key:'takeNumber',label:'Numero take',type:'number'},{key:'audioUrl',label:'URL audio',type:'url'},{key:'notes',label:'Note',type:'textarea'}]};
export const RECORDING_SESSION_ROUTES:Routes=[{path:'',component:ResourceListPage,data:{resource}},{path:'nuova',component:RecordingSessionFormPage,data:{resource}},{path:'new',redirectTo:'nuova',pathMatch:'full'},{path:':id/edit',redirectTo:':id/modifica',pathMatch:'full'},{path:':id/modifica',component:RecordingSessionFormPage,data:{resource}},{path:':id',component:ResourceDetailPage,data:{resource}}];
