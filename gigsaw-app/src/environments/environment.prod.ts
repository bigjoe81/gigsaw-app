export const environment = {
  production: true,
  // Public Laravel backend origin.
  API_URL: 'https://api.gigsaw.it',
  apiPath: '/api/v1',
  googleAuthPath: '/auth/google/redirect',
  mapboxAccessToken: '',
};
